# 18: ML

Журнал запусков моделей: https://docs.google.com/spreadsheets/d/1xD_tEizBcZ15uNz0WjzWm11pJHIbdY5ciPegAEDLorM/edit?usp=sharing

## Ноутбуки

Тренировка модели: [train_model.ipynb](train_model.ipynb)

Inference на сохранённых весах: [inference.ipynb](inference.ipynb)

Оба ноутбука находятся в корневой папке проекта.

## Требования

* Python 3.9

### Установка зависимостей

Рекомендуется создать виртуальное окружение.

Затем установите необходимые зависимости.

```bash
pip install -r requirements.txt
```

Если вы используете виртуальное окружение, убедитесь, что оно активировано перед выполнением вышеуказанной команды.

## Структура проекта

Вот краткий обзор структуры каталогов проекта:

```
src/
│
├── models/
│   ├── __all_models.py
│   └── ...
│
├── utils/
│   ├── logging.py
│   ├── plotting.py
│   └── engine.py
│
├── launcher/
│   ├── configs/
│   ├── models/
│   ├── launched_trainer.py
│   ├── launcher.py
│   └── engine.py
├── notebooks/
│   ├── train_model.ipnb
│   └── inference.ipnb
│
├── dataloader.py
├── inference.py
└── train.py
```

## Описание файлов

### `train.py`

Этот файл является входом для обучения одной модели. Действия для запуска одной модели указаны ниже.

### `models/`

Этот каталог содержит определения моделей, используемых в проекте.

- **`__all_models.py`**: Этот файл импортирует и экспортирует все доступные архитектуры моделей в репозитории. Его можно
  использовать для удобного доступа к любой модели, определенной в этом каталоге.

### `utils/`

Этот каталог содержит функции, которые помогают в различных аспектах проекта.

- **`engine.py`**: Функции настройки тренера и оценщиков.


- **`logging.py`**: Логирование результатов обучения и оценки модели


- **`plotting.py`**: Визуализация метрик обучения

### `dataloader.py`

В этом файле находятся функции чтения и предварительной
обработки данных разделения на обучающую, валидационную,
тестовую выборки, аугментации.

## Обучение одной модели

Чтобы обучить модель, выполните следующие шаги:

1. Настройте окружение, установив необходимые зависимости (как указано выше).
2. Скачайте и распакуйте набор данных с kaggle
3. В файле `config.py` укажите расположение папки с данными, папок сохранения результатов и весов, выберите оптимизатор, задайте гиперпараметры обучения
4. Выберите модель в файле `train.py`.
5. Запустите файл `train.py`. Для тестового запуска рекомендуется запустить модель на уменьшенном датасете.
6. Смотрите результаты тренировки и сохранённые весы в папке, указанной в конфиге.

## Обучение нескольких моделей

Чтобы обучить несколько моделей подряд, выполните следующие шаги:
1. Добавьте конфиги моделей для запуска в папку `launcher/configs` .
2. Добавьте модели для запуска в папку `launcher/models`, 
импортируйте модели в файле `__all_models.py`.
3. Импортируйте конфиги в файл `train_launcher.py`.
4. В файле `train_launcher.py` добавьте модель и конфиг в `launch_list`, 
если необходимо исключить некоторые классы из датасета, создайте `list` 
и добавьте его в `launch_list`.
5. Запустите `train_launcher.py`.
6. Смотрите результаты тренировки и сохранённые весы в папке, указанной в конфиге.
